To Merge code we have 2 way.

Way 1
Creating a PR (pull reguest) in github.

Way 2
git diff <- branch name -> (to compare commits, branches, files & more)
git merge <- branch name -> (to merge to branches)

---------------------------

To resolve merge conflict.
